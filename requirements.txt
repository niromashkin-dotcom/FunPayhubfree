# Основные зависимости FunPayHub
requests==2.31.0
aiohttp==3.9.1
flask==3.0.0
cryptography==41.0.7
python-dotenv==1.0.0
schedule==1.2.0
pydantic==2.5.0

# Обязательны для FunPayAPI (скрапинг funpay.com) — без них account.py не импортируется
beautifulsoup4==4.12.3
requests-toolbelt==1.0.0
urllib3==2.2.1

# Обязательны для tg_bot_service.py (Control Panel бот, отдельный процесс)
pyTelegramBotAPI==4.14.0
bcrypt==4.1.2
psutil==5.9.8

# Опционально используется AutoSMM-плагином для TTL-кэша (есть fallback, если не установлен)
expiringdict==1.2.2

# Дополнительные утилиты
pillow==10.2.0
sqlalchemy==2.0.23
alembic==1.13.1

# Для мониторинга и логирования
prometheus-client==0.19.0
structlog==23.2.0

# Для веб-интерфейса
jinja2==3.1.3

pytest>=7.0.0
pytest-cov>=4.0.0

# Production WSGI сервер для Flask
gunicorn==21.2.0
